{
  "name": "after-credits-site",
  "version": "1.0.0",
  "private": true,
  "description": "マーベル・アフタークレジット — 映画・ドラマ・コミック総合ガイド(静的サイト)",
  "scripts": {
    "validate": "node scripts/validate-data.js",
    "build": "npm run validate && eleventy",
    "serve": "npm run validate && eleventy --serve",
    "clean": "node -e \"fs.rmSync('_site',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
